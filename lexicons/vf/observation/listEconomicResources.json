{
  "lexicon": 1,
  "id": "vf.observation.listEconomicResources",
  "defs": {
    "main": {
      "type": "query",
      "description": "List economicResources. Filterable by: containedIn, contains, primaryAccountable, currentLocation, ofBatchLot, unitOfEffort, conformsTo, stage.",
      "parameters": {
        "type": "params",
        "properties": {
          "uri": {
            "type": "string",
            "format": "at-uri",
            "description": "Fetch a single record by AT-URI. Other filters are ignored when set."
          },
          "containedIn": {
            "type": "string",
            "description": "Filter where containedIn array contains this AT-URI.",
            "format": "at-uri"
          },
          "contains": {
            "type": "string",
            "description": "Filter where contains array contains this AT-URI.",
            "format": "at-uri"
          },
          "primaryAccountable": {
            "type": "string",
            "description": "Filter by primaryAccountable (DID of referenced agent).",
            "format": "did"
          },
          "currentLocation": {
            "type": "string",
            "description": "Filter by currentLocation (AT-URI of referenced record).",
            "format": "at-uri"
          },
          "ofBatchLot": {
            "type": "string",
            "description": "Filter by ofBatchLot (AT-URI of referenced record).",
            "format": "at-uri"
          },
          "unitOfEffort": {
            "type": "string",
            "description": "Filter by unitOfEffort (AT-URI of referenced record).",
            "format": "at-uri"
          },
          "conformsTo": {
            "type": "string",
            "description": "Filter by conformsTo (AT-URI of referenced record).",
            "format": "at-uri"
          },
          "stage": {
            "type": "string",
            "description": "Filter by stage (AT-URI of referenced record).",
            "format": "at-uri"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 50,
            "description": "Maximum number of records to return."
          },
          "cursor": {
            "type": "string",
            "description": "Pagination cursor from a previous response."
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": [
            "records"
          ],
          "properties": {
            "records": {
              "type": "array",
              "items": {
                "type": "object",
                "required": [
                  "uri"
                ],
                "properties": {
                  "uri": {
                    "type": "string",
                    "format": "at-uri",
                    "description": "AT-URI of the record."
                  },
                  "value": {
                    "type": "unknown",
                    "description": "The full record value."
                  }
                }
              }
            },
            "cursor": {
              "type": "string",
              "description": "Pagination cursor for next page."
            }
          }
        }
      }
    }
  }
}
