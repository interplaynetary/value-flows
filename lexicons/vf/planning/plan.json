{
  "lexicon": 1,
  "id": "vf.planning.plan",
  "description": "A logical collection of processes, with optional connected agreements, that constitute a body of scheduled work with defined deliverable(s).",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["name"],
        "properties": {
          "name": {
            "type": "string",
            "description": "An informal or formal textual identifier for the plan."
          },
          "due": {
            "type": "string",
            "format": "datetime",
            "description": "The date/time the plan is expected to be complete."
          },
          "created": {
            "type": "string",
            "format": "datetime",
            "description": "The date/time the plan was created."
          },
          "planIncludes": {
            "type": "array",
            "items": { "type": "string", "format": "at-uri" },
            "description": "References to the processes, commitments, and intents that constitute this plan."
          },
          "hasIndependentDemand": {
            "type": "array",
            "items": { "type": "string", "format": "at-uri" },
            "description": "References to vf.planning.commitment records this plan was created to deliver."
          },
          "note": {
            "type": "string",
            "description": "Any useful textual information about the plan."
          }
        }
      }
    }
  }
}
