{
  "lexicon": 1,
  "id": "vf.planning.listAgreements",
  "defs": {
    "main": {
      "type": "query",
      "description": "List agreements. Filterable by: stipulates, stipulatesReciprocal, realizes, realizesReciprocal, bundledIn.",
      "parameters": {
        "type": "params",
        "properties": {
          "uri": {
            "type": "string",
            "format": "at-uri",
            "description": "Fetch a single record by AT-URI. Other filters are ignored when set."
          },
          "stipulates": {
            "type": "string",
            "description": "Filter where stipulates array contains this AT-URI.",
            "format": "at-uri"
          },
          "stipulatesReciprocal": {
            "type": "string",
            "description": "Filter where stipulatesReciprocal array contains this AT-URI.",
            "format": "at-uri"
          },
          "realizes": {
            "type": "string",
            "description": "Filter where realizes array contains this AT-URI.",
            "format": "at-uri"
          },
          "realizesReciprocal": {
            "type": "string",
            "description": "Filter where realizesReciprocal array contains this AT-URI.",
            "format": "at-uri"
          },
          "bundledIn": {
            "type": "string",
            "description": "Filter by bundledIn (AT-URI of referenced record).",
            "format": "at-uri"
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 50,
            "description": "Maximum number of records to return."
          },
          "cursor": {
            "type": "string",
            "description": "Pagination cursor from a previous response."
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": [
            "records"
          ],
          "properties": {
            "records": {
              "type": "array",
              "items": {
                "type": "object",
                "required": [
                  "uri"
                ],
                "properties": {
                  "uri": {
                    "type": "string",
                    "format": "at-uri",
                    "description": "AT-URI of the record."
                  },
                  "value": {
                    "type": "unknown",
                    "description": "The full record value."
                  }
                }
              }
            },
            "cursor": {
              "type": "string",
              "description": "Pagination cursor for next page."
            }
          }
        }
      }
    }
  }
}
